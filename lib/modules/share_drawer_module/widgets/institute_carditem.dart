import 'package:flutter/material.dart';

class InstituteCardItem extends StatelessWidget {
  const InstituteCardItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}